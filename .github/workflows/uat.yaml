on:
  push:
    branches: dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Obtener version de la aplicación
        id: version_name
        uses: ./.github/actions/action.yml
        with:
          env: 'UAT'     
    
      - name: build-image
        uses: ./.github/actions/ci-cd.yml
        with:
          env: 'UAT'    
    

